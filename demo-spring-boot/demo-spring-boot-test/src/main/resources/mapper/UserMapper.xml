<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wigo.demo.spring.boot.test.dao.UserMapper">

    <select id="selectById" resultType="org.wigo.demo.spring.boot.test.entity.User">
        select * from t_user where id = #{id}
    </select>

    <select id="selectById2" resultType="org.wigo.demo.spring.boot.test.entity.User">
        select * from t_user where id = ${id}
    </select>
</mapper>
